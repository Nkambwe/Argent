namespace Argent.Api.Domain.Enums {
    public enum PostingType {
        Direct = 1,   // manual / direct posting allowed
        System = 2,   // only system-generated postings
        Both = 3
    }
}
